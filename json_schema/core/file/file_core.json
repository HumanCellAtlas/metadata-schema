{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "http://schema.humancellatlas.org/core/file/4.0.0/file_core.json",
    "description": "A file contains information about the data files produced from single cells.",
    "additionalProperties": false,
    "required": [
	    "filename"
    ],
    "title": "file_core",
    "properties": {
        "filename": {
            "description": "A filename for a data file. Must have a valid extension.",
            "pattern": "^.*.fastq$|^.*.fastq.gz$|^.*.jpg$|^.*.jpeg$|^.*.tiff$|^.*.png$",
            "type": "string"
        },
        "file_format": {
            "description": "The format that the data file is in. Must be one of: fastq, fastq.gz, tiff, jpg, png.",
            "enum": [
                "fastq",
                "fastq.gz",
                "tiff",
                "jpg",
                "png"
            ]
        }
    },
    "type": "object"
}
