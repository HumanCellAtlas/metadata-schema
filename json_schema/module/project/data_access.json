{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Information about the data access type for the project.",
  "additionalProperties": false,
  "required": [
    "type"
  ],
  "if": {
    "properties": {
      "type": {
        "const": "Complicated/see notes"
      }
    }
  },
  "then": {
    "required": [
      "notes"
    ]
  },
  "title": "Data access",
  "name": "data_access",
  "type": "object",
  "properties": {
    "describedBy": {
      "description": "The URL reference to the schema.",
      "type": "string",
      "pattern" : "^(http|https)://schema.(.*?)humancellatlas.org/module/project/(([0-9]{1,}.[0-9]{1,}.[0-9]{1,})|([a-zA-Z]*?))/data_access"
    },
    "schema_version": {
      "description": "The version number of the schema in major.minor.patch format.",
      "type": "string",
      "pattern": "^[0-9]{1,}.[0-9]{1,}.[0-9]{1,}$",
      "example": "4.6.1"
    },
    "type": {
      "description": "Type of data access permissions associated with this project.",
      "type": "string",
      "enum": [
        "All fully open",
        "All managed access",
        "A mixture of open and managed",
        "Complicated/see notes"
      ],
      "guidelines": "Must be one of: All fully open,All managed access,A mixture of open and managed,Complicated/see notes",
      "user_friendly": "Data access type"
    },
    "notes": {
      "description": "Notes regarding data access type",
      "type": "string",
      "example": "This dataset can only be accessed through contact with the authors",
      "user_friendly": "Data access notes"
    }
  }
}
