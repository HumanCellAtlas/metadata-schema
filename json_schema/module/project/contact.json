{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "http://schema.humancellatlas.org/module/project/4.0.0/contact.json",
    "description": "Information about a person who submitted or contributed to a project.",
    "additionalProperties": false,
    "required": [
        "name",
        "email"
    ],
    "title": "contact",
    "type": "object",
    "properties": {
        "$schema" : {
            "type": "string",
            "pattern" : "http://schema.humancellatlas.org/module/project/[0-9]{1,}.[0-9]{1,}.[0-9]{1,}/contact.json"
        },
        "schema_version": {
            "description": "Version number in major.minor.patch format.",
            "type": "string",
            "pattern": "^[0-9]{1,}.[0-9]{1,}.[0-9]{1,}$",
            "example": "4.6.1"
        },
        "contact_name": {
            "type": "string",
            "description": "The contact's name. Should be in the format first, middle, last name. Middle can be initial or left blank. e.g. John,D,Doe or Jane,,Smith",
            "user_friendly": "Contact name"
        },
        "email": {
            "pattern": "^.*@.*..*$",
            "type": "string",
            "description": "An email address for the contact.",
            "user_friendly": "Email"
        },
        "phone": {
            "type": "string",
            "description": "Phone number (including country code) of contact or contact's lab.",
            "user_friendly": "Phone number"
        },
        "institution": {
            "type": "string",
            "description": "Name of primary institute where contact works.",
            "user_friendly": "Institution"
        },
        "laboratory": {
            "type": "string",
            "description": "Name of lab (often the PI name) within institute where contact works.",
            "user_friendly": "Laboratory"
        },
        "address": {
            "type": "string",
            "description": "Full mailing address where contact works. Should include street name and number, city, state, postal code, country.",
            "user_friendly": "Address"
        },
        "city": {
            "type": "string", 
            "description": "Name of city where contact works.",
            "user_friendly": "City"
        }, 
        "country_division": {
            "type": "string",
            "description": "Name of state, province, canton, or other country subdivision where contact works.",
            "user_friendly": "Country division"
        },
        "country": {
            "type": "string", 
            "description": "Name of country where contact works.",
            "user_friendly": "Country"
        }
    }
}
