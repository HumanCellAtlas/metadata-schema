{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id" : "http://schema.humancellatlas.org/common/ontology/4.0.0/disease_ontology.json",
    "description": "A term that may be associated with a disease-related ontology term",
    "additionalProperties": false,
     "required": [
        "text"
    ],
    "title": "disease_ontology",
    "type": "object",
    "properties": {
        "$schema" : {
            "pattern" : "http://schema.humancellatlas.org/common/ontology/[0-9]{1,}.[0-9]{1,}.[0-9]{1,}/disease_ontology.json",
            "type": "string"
        },
        "text": {
            "description": "The text for the term as the user provides it.",
            "type": "string"
        },
        "ontology": {
            "description": "An optional ontology reference in format where prefix_ indicates which ontology",
            "type": "string"
        }
    }
}