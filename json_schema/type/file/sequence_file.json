{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "http://schema.humancellatlas.org/type/file/4.0.0/sequence_file.json",
    "description": "A file of read sequences generated by a sequencing experiment.",
    "additionalProperties": false,
    "required": [
        "read_index"
    ],
    "title": "sequence_file",
    "type": "object",
    "properties": {
        "$schema": {
            "pattern": "http://schema.humancellatlas.org/module/process/sequencing/[0-9]{1,}.[0-9]{1,}.[0-9]{1,}/sequence_file.json",
            "type": "string"
        },
        "schema_version": {
            "description": "Version number in major.minor.patch format.",
            "type": "string",
            "pattern": "^[0-9]{1,}.[0-9]{1,}.[0-9]{1,}$",
            "example": "4.6.1"
        },
        "read_index": {
            "description": "Whether the read represents the R1, R2, I1, or I2 part of the sequencing read.",
            "type": "string",
            "enum": [
                "r1",
                "r2",
                "i1",
                "i2"
            ]
        },
        "lane_index": {
            "description": "The index of the lane that this file was produced from.",
            "type": "integer"
        }
    }
}
