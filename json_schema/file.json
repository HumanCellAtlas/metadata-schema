{
    "description": "Information about the data files produced from single cells.",
    "title": "file",
    "required": [
        "filename"
    ],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "properties": {
        "core": {
            "description": "Type and schema for this object.",
            "$ref": "https://raw.githubusercontent.com/HumanCellAtlas/metadata-schema/4.5.0/json_schema/core.json"
        },
        "filename": {
            "pattern": "^.*.fastq$|^.*.fastq.gz$|^.*.jpg$|^.*.jpeg$|^.*.tiff$|^.*.png$",
            "type": "string",
            "description": "A filename for a data file. Must have a valid extension."
        },
        "file_format": {
            "enum": [
                "fastq",
                "fastq.gz",
                "tiff",
                "jpg",
                "png"
            ],
            "description": "The format that the data file is in. Must be one of: fastq, fastq.gz, tiff, jpg, png."
        }
    }
}