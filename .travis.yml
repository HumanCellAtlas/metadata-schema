language:
   - node_js
   - python

# python version to be used for testing
node_js:
   - "8"

python:
   - "3.6"

install:
   - pip install -r requirements.txt
   - cd tests
   - npm install

script:
   - npm test
   - cd src
   # run schema linter
   - python schema_linter.py
