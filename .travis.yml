language:
   - node_js
   - python

node_js:
   - "8"

# python version to be used for linter
python:
   - "3.6"

install:
#   - pip install -r requirements.txt
   - cd tests
   - npm install

script:
   - npm test
   # run schema linter
   - cd ../src
   - python schema_linter.py
